version: '3.8'

services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - books_data:/data
    environment:
      - NODE_ENV=production
      - DATA_DIR=/data
      - PORT=3000
    restart: unless-stopped

volumes:
  books_data:
